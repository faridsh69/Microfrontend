import React from "react";
import ReactDOM from "react-dom/client";
// static import
import MyApp from "remote/App";

ReactDOM.createRoot(document.getElementById("root")).render(
  <React.StrictMode>
    <MyApp />
  </React.StrictMode>
);
