import React from "react";
import ReactDOM from "react-dom/client";
import VitRemoteComponent from "./VitRemoteComponent.jsx";

ReactDOM.createRoot(document.getElementById("root")).render(
  <React.StrictMode>
    <VitRemoteComponent />
  </React.StrictMode>
);
